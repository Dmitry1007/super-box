<div class="row">
  <div class="medium-3 columns">
    <%= form_for(:idea, url: ideas_path) do |f| %>
      <%= f.email_field :title %>
      <%= f.password_field :body %>

      <%= f.submit "Save" %>
    <% end %>
  </div>
</div>
